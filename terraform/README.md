1. from ./terraform run: ```terraform apply -var-file="secrets.tfvars"```

Use ```terraform plan``` to see provisioned changes before applying new configurations.

Terraform providers should be downloaded automatically.