#!/usr/bin/env groovy

pipeline {
  agent any
  stages {
    stage('Run K8s Tests') {
      steps {
        script {
          // Verify cluster access
          sh 'kubectl cluster-info'
          
        }
      }
    }
  }
}